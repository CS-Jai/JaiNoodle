using UnityEngine;

public class spaceScripts : MonoBehaviour
{

    public bool available;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        available = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
